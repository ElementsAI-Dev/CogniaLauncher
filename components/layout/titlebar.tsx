/**
 * @deprecated The standalone Titlebar is no longer rendered.
 * Window controls are now embedded in the app header via
 * `<WindowControls />` and `useWindowControls()`.
 *
 * This file is kept only for backward compatibility with tests
 * and any lingering imports.
 */
export function Titlebar() {
  return null;
}
