[package]
name = "builtin_semver_inspector"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
cognia-plugin-sdk = { path = "../../../plugin-sdk" }
extism-pdk = "1.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
semver = "1"
