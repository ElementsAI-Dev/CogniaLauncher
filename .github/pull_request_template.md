## Summary

- What changed:
- Why:

## Validation

- [ ] `pnpm lint`
- [ ] `pnpm test`
- [ ] `pnpm docs:validate` (or `pnpm docs:validate:warn` during rollout checks)
- [ ] Additional checks (if needed): `cargo check` / `cargo test` / e2e subset

## Docs Impact

- [ ] Docs updated in this PR (include capability IDs and affected `docs/en/**` + `docs/zh/**` paths)
- [ ] No docs impact (capability-scoped justification provided)

Impacted capability IDs (from `docs/documentation-coverage-matrix.json`):

- 

Updated docs paths:

- `docs/en/...`
- `docs/zh/...`

If "No docs impact" is selected, provide justification:

<!-- Explain why user-facing behavior and developer workflow docs are unaffected, and list the capability IDs reviewed. -->

## Reviewer Notes

- Areas needing extra attention:
- Known risks/trade-offs:
